---
"mp-pong-2020": minor
---

Replace the vanilla-JS game with a Vite + TypeScript + Three.js project skeleton (Milestone 0): `npm run dev` renders a rotating placeholder cube, `npm run build` produces a static bundle. Adds ESLint/Prettier and Vitest tooling per TECHSTACK.md.
