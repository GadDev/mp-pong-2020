/**
 * The on-screen game title. Trialling EXCHANGE — see docs/EXPLORATION.md §1.
 * Not yet spec: the title question is still open, so this stays a single
 * constant rather than being inlined at each mark.
 */
export const GAME_TITLE = "EXCHANGE";
